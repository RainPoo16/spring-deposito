---
apiVersion: v1
kind: Secret
metadata:
  name: deposit-db
type: servicebinding.io/postgresql
stringData:
  type: "postgresql"
  provider: "postgresql"
  host: "deposit-db"
  port: "5432"
  database: "deposito"
  username: "deposito"
  password: "deposito"

---
apiVersion: v1
kind: Service
metadata:
  name: deposit-db
spec:
  ports:
    - port: 5432
  selector:
    app: deposit-db

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deposit-db
  labels:
    app: deposit-db
spec:
  selector:
    matchLabels:
      app: deposit-db
  template:
    metadata:
      labels:
        app: deposit-db
    spec:
      containers:
        - image: postgres:18.1
          name: postgresql
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: deposit-db
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: deposit-db
                  key: password
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: deposit-db
                  key: database
          ports:
            - containerPort: 5432
              name: postgresql
          livenessProbe:
            tcpSocket:
              port: postgresql
          readinessProbe:
            tcpSocket:
              port: postgresql
          startupProbe:
            tcpSocket:
              port: postgresql
