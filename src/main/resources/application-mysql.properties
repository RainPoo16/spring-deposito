# database init, supports mysql too
database=mysql
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost/deposito}
spring.datasource.username=${MYSQL_USER:deposito}
spring.datasource.password=${MYSQL_PASS:deposito}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
