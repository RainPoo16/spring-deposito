name: Deploy and Test Cluster

on:
  push:
    branches: [main]
    paths:
      - 'k8s/**'
  pull_request:
    branches: [main]
    paths:
      - 'k8s/**'

jobs:
  deploy-and-test-cluster:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v2

      - name: Create k8s Kind Cluster
        uses: helm/kind-action@v1

      - name: Build application image
        run: |
          docker build -t bank-deposit-service:latest .

      - name: Load image into Kind
        run: |
          kind load docker-image bank-deposit-service:latest

      - name: Deploy application
        run: |
          kubectl apply -f k8s/

      - name: Wait for Pods to be ready
        run: |
          kubectl wait --for=condition=ready pod -l app=deposit-db --timeout=180s
          kubectl wait --for=condition=ready pod -l app=deposit-service --timeout=180s

